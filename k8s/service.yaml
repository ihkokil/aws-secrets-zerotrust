apiVersion: v1
kind: Service
metadata:
  name: secrets-demo-service
  namespace: secrets-demo
  labels:
    app: secrets-demo
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app: secrets-demo
